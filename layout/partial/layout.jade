doctype
html(lang=config.language)
    head
        include head
    body
        #app.with-aside
            include header
            aside
                a.logo-link(href=url_for())
                    img(src=theme.logo)
                    span= theme.title
                include aside
                block footer
            section.main
                block container
                block pagination
                
        include scripts